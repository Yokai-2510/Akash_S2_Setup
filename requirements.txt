# ----------------------------------------------------
# S2 TRADING SYSTEM - PYTHON DEPENDENCIES
#
# To install all dependencies, run:
# pip install -r requirements.txt
# ----------------------------------------------------

# --- Core Data Science & Manipulation ---
pandas==2.2.2             # For data manipulation (DataFrames)
numpy==1.26.4             # Foundational package for scientific computing, dependency for pandas

# --- API Communication ---
requests==2.31.0          # For making HTTP requests to Upstox and NSE APIs

# --- Technical Analysis ---
pandas-ta==0.3.14b0       # Library for calculating technical indicators easily

# --- Google Sheets Integration ---
gspread==5.12.4           # For interacting with the Google Sheets API
gspread-dataframe==3.3.1  # For easy conversion between gspread worksheets and pandas DataFrames
oauth2client==4.1.3       # For handling Google API authentication

# --- Broker Login & Automation (Upstox) ---
playwright==1.44.0        # For browser automation to handle the Upstox login flow
pyotp==2.9.0              # For generating Time-based One-Time Passwords (TOTP) for login
pytz==2024.1              # For handling timezones, especially for token expiry

# --- Utilities & Helpers ---
tqdm==4.66.4              # For creating smart progress bars during data downloads
openpyxl==3.1.2           # Required by pandas to write to and format .xlsx files (for Google Sheets output)